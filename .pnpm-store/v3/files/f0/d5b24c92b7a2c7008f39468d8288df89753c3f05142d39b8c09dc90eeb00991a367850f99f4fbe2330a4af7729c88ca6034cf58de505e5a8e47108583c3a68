export default reorder;
declare namespace reorder {
    namespace reorder {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
