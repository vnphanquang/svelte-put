export default heartbeat;
declare namespace heartbeat {
    namespace heartbeat {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
