export default columns;
declare namespace columns {
    namespace columns {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
