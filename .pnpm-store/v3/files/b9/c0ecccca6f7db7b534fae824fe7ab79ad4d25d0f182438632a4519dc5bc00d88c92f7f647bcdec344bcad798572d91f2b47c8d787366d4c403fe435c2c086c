export default pagelines;
declare namespace pagelines {
    namespace pagelines {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
