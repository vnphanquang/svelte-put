import { definePackageMetadata } from '$packages/utils';

export default definePackageMetadata({
	publishedAt: 1717521500513,
	description:
		'svelte preprocessor to add appropriate attributes to anchor tags for external links',
});
