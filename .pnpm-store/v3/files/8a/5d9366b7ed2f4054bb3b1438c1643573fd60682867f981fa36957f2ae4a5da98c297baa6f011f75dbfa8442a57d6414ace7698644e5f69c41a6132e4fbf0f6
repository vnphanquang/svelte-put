export default refresh;
declare namespace refresh {
    namespace refresh {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
