export default compass;
declare namespace compass {
    namespace compass {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
