export default viacoin;
declare namespace viacoin {
    namespace viacoin {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
