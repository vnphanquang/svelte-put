export default compress;
declare namespace compress {
    namespace compress {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
