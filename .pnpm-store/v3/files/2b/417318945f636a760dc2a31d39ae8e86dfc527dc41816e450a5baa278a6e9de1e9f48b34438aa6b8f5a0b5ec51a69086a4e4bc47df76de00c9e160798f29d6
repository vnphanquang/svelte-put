export default database;
declare namespace database {
    namespace database {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
