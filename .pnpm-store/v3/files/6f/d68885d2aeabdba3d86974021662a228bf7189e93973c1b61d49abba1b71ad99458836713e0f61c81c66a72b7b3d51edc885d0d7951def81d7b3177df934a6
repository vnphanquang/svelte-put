export default deafness;
declare namespace deafness {
    namespace deafness {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
