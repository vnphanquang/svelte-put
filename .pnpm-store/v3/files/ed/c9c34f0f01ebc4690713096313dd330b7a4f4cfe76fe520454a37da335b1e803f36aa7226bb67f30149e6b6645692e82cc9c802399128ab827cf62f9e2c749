export default registered;
declare namespace registered {
    namespace registered {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
