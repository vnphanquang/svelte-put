export default comment;
declare namespace comment {
    namespace comment {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
