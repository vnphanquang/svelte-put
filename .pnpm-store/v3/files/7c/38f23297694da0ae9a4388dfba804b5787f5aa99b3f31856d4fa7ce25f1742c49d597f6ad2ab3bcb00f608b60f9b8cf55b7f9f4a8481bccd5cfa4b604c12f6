export default asterisk;
declare namespace asterisk {
    namespace asterisk {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
