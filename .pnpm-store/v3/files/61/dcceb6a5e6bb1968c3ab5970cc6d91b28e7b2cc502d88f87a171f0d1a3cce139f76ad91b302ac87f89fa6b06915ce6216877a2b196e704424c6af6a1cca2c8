export default delicious;
declare namespace delicious {
    namespace delicious {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
