export default comments;
declare namespace comments {
    namespace comments {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
