export default genderless;
declare namespace genderless {
    namespace genderless {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
