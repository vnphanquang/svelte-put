export default cutlery;
declare namespace cutlery {
    namespace cutlery {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
