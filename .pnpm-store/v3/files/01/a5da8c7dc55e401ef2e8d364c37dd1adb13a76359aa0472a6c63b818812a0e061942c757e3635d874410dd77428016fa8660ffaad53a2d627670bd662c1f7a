export default wpforms;
declare namespace wpforms {
    namespace wpforms {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
