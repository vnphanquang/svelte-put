export default eercast;
declare namespace eercast {
    namespace eercast {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
