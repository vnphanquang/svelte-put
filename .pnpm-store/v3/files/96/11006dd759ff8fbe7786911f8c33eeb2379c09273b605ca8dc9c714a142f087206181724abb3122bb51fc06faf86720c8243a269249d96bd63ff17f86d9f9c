export default wpbeginner;
declare namespace wpbeginner {
    namespace wpbeginner {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
