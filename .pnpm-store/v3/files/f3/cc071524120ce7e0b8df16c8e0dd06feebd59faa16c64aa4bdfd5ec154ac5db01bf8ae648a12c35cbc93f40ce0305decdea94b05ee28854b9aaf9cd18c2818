export default underline;
declare namespace underline {
    namespace underline {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
