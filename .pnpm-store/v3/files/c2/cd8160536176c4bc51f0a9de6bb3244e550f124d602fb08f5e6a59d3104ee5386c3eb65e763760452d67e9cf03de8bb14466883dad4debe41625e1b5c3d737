export default forward;
declare namespace forward {
    namespace forward {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
