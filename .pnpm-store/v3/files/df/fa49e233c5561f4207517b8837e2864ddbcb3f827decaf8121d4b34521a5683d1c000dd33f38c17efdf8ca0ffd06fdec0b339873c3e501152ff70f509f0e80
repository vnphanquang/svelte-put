export default spinner;
declare namespace spinner {
    namespace spinner {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
