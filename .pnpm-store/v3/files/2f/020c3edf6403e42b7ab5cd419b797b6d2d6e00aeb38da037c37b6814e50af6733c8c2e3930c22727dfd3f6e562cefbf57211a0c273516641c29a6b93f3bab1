export default windows;
declare namespace windows {
    namespace windows {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
