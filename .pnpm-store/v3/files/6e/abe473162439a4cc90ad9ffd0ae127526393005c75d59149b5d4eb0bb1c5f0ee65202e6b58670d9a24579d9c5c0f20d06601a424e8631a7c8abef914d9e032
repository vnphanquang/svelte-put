export default certificate;
declare namespace certificate {
    namespace certificate {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
