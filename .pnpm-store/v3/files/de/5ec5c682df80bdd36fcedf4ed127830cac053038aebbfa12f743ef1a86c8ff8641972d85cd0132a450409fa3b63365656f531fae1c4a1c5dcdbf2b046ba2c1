export default linkedin;
declare namespace linkedin {
    namespace linkedin {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
