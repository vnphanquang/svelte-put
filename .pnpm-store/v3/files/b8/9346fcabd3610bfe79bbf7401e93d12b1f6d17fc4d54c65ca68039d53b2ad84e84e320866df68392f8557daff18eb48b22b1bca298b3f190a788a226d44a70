export default sitemap;
declare namespace sitemap {
    namespace sitemap {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
