export default history;
declare namespace history {
    namespace history {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
