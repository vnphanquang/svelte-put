export default question;
declare namespace question {
    namespace question {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
