export default exchange;
declare namespace exchange {
    namespace exchange {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
