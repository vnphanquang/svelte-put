export default institution;
declare namespace institution {
    namespace institution {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
