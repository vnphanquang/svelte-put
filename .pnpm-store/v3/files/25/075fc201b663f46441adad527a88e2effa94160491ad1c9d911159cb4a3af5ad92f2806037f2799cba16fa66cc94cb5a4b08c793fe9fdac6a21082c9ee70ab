export default soundcloud;
declare namespace soundcloud {
    namespace soundcloud {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
