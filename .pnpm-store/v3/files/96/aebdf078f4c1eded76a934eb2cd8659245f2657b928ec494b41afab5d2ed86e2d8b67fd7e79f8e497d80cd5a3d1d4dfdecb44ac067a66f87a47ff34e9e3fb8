export default language;
declare namespace language {
    namespace language {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
