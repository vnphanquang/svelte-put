export default android;
declare namespace android {
    namespace android {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
