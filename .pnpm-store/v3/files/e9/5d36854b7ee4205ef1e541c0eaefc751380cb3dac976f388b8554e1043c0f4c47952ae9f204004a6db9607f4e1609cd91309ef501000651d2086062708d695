export default sliders;
declare namespace sliders {
    namespace sliders {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
