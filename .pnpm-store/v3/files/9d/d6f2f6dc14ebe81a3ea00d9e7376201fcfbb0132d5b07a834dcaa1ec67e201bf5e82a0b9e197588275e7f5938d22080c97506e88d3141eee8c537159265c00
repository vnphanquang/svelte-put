export default support;
declare namespace support {
    namespace support {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
