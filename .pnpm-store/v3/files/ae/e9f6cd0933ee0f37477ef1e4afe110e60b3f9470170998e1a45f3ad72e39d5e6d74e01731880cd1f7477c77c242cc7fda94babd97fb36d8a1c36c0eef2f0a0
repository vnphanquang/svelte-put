export default percent;
declare namespace percent {
    namespace percent {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
