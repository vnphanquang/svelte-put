export default bitbucket;
declare namespace bitbucket {
    namespace bitbucket {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
