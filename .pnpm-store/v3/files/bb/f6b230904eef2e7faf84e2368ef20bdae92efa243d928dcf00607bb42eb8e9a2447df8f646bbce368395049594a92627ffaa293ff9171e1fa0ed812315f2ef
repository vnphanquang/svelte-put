export default bluetooth;
declare namespace bluetooth {
    namespace bluetooth {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
}
