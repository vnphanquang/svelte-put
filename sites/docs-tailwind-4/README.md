# svelte-put documentation site

## Development

### Search

Search is implemented with [pagefind]. To enable search in development, run `pnpm build` once before `pnpm dev`. To re-index search, run `pnpm build` again.

[pagefind]: https://pagefind.app/
